# Virtual environment
.venv/

# Claude configuration
.claude/

# Python
__pycache__/
*.py[cod]
*.pyo
.ipynb_checkpoints/

# Data — never commit data files (all data lives in GCS)
*.nc
*.csv
# Rangeland source CSVs are local (not GCS) — tracked at 3 dp precision
!RangeSTAR_data/*.csv
*.npy
*.npz
*.pkl
*.parquet
*.pt
*.png
*.parquet

# files
outputs/paper_draft.md

# Credentials
*.json
.env
gcloud_credentials/

# OS
.DS_Store
Thumbs.db

# Folders
# RangeSTAR_data/ — NOT ignored; CSVs tracked via !RangeSTAR_data/*.csv exception above

# Pipeline outputs (artifacts already ignored by extension; ignore the trees too)
outputs/

# MLflow experiment tracking (files have no extension — must ignore the dir)
mlruns/